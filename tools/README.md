# Tools

Operational scripts may live here, but credentials must be provided at runtime through environment variables or a secure secret store.

Never commit credentials, `wp-config.php`, database dumps, or FTP logs.
